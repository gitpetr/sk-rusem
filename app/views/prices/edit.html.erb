<% if user_signed_in? %>
  <% if current_user.is_admin %>

<h1>Editing Price</h1>

<%= render 'form' %>

 
<%= link_to 'Back', prices_path %>
<% else %>
  <%= render 'access' %>
<% end %>
  
<% end %>