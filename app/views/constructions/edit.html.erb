<% if user_signed_in? %>
  <% if current_user.is_admin %>
  <h1>Editing Construction</h1>

  <%= render 'form' %>

  <%= link_to 'Show', @construction %> |
  <%= link_to 'Back', constructions_path %>
<% else %>
  <%= render 'access' %>
<% end %>
  
<% end %>