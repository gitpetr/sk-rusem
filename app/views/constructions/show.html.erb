

<div class="container">
  <div class="row whitefone">

<p>
  
  <%= @construction.title %>
</p>

<p>
   
   <%= image_tag @construction.image.url(:medium) %>
  <%= simple_format @construction.body %>
</p>
<% if user_signed_in? %>
  <% if current_user.is_admin %>
    <%= link_to 'Edit', edit_construction_path(@construction) %> |
<% end %>
<% end %>
<%= link_to 'Back', constructions_path %>

</div>
  </div>