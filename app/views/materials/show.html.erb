
<div class="container">
  <div class="row whitefone">

<p>

  <%= @material.title %>
</p>

<p>
 <%= image_tag @material.image.url(:medium) %>
  <%= simple_format @material.body %>
</p>

<% if user_signed_in? %>
  <% if current_user.is_admin %>
<%= link_to 'Edit', edit_material_path(@material), class: "btn btn-warning btn-sm pull-right" %> <span class=" pull-right"> &nbsp; </span>
<% end %>
<% end %>

<%= link_to 'Back', materials_path, class: "btn btn-success btn-sm pull-right" %>
</div>
  </div>