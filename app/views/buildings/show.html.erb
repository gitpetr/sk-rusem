<div class="container">
  <div class="row whitefone">

<p>
 
  <%= @building.title %>
</p>

<p>
<%= image_tag @building.image.url(:medium) %>
  <%= simple_format @building.body %>
</p>
<% if user_signed_in? %>
  <% if current_user.is_admin %>
<%= link_to 'Изменить', edit_building_path(@building), class: "btn btn-warning btn-sm pull-right" %> <span class="pull-right">&nbsp;</span>
<% end %>
<% end %>

<%= link_to 'Назад', buildings_path, class: "btn btn-success btn-sm pull-right" %>
</div>
  </div>