<% if user_signed_in? %>
  <% if current_user.is_admin %>
  
<h1>Editing News</h1>

<%= render 'form' %>

<%= link_to 'Show', @news %> |
<%= link_to 'Back', news_index_path %>
<% else %>
  <%= render 'access' %>
<% end %>
  
<% end %>